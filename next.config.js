/** @type {import('next').NextConfig} */
const nextConfig = {
  // This config is for the root level - actual Next.js app is in hikaru-nextjs
  // Vercel will use hikaru-nextjs as rootDirectory per vercel.json
}

module.exports = nextConfig